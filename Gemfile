# frozen_string_literal: true

source "https://rubygems.org"

gem "jekyll", "~> 3.9"       # or 4.3 if you're not limited by GitHub Pages version
gem "jekyll-theme-minimal"
gem "rouge"                  # syntax highlighter
gem "jekyll-paginate"        # optional plugin
gem "jekyll-sitemap"         # optional plugin
gem "kramdown-parser-gfm"
gem "base64"
gem "bigdecimal"

# allow local _plugins folder
group :jekyll_plugins do
  # no need to list custom plugins here — Jekyll auto-loads _plugins/*.rb
end
